# Google Gemini API key — used server-side only for personalised insights.
# Get one free at https://aistudio.google.com/apikey
# The app still works without it (it falls back to built-in guidance),
# but set this to enable AI-personalised insights.
GEMINI_API_KEY=
